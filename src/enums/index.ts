export enum ButtonSize {
	mini = 'mini',
	small = 'small',
	large = 'large',
}
export enum ButtonType {
	default = 'default',
	success = 'success',
	disabled = 'disabled',
	warning = 'warning',
	danger = 'danger',
}

export enum ButtonDisabled {
	disabled = 'disabled',
}
